from Flask import Flask
from Threading import Thread

app = Flask('');

@app.rounte('/');
def home('');
    return":)"


def_run();
    app.run(host="0.0.0.0", port="8080")
  
  
  def keep_alive(): 
      t= Thread(target-run)
      t.start()
